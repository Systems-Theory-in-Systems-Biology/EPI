myst-parser>=0.18
sphinx_rtd_theme>=1.2.0
docutils>=0.18
sphinx_copybutton
# Install epi and its dependencies so sphinx can generate the docs.
-e .
pytest #TODO: Install project with all dev dependencies. This is just a fix for now, as pytest is the only imported dev dependency.
